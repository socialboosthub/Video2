const express=require('express');const path=require('path');const app=express();app.use(express.json({limit:'2mb'}));app.use(express.static(__dirname));app.get('/health',(req,res)=>res.json({ok:true,service:'AHM Studio V6'}));app.post('/api/generate',(req,res)=>{res.status(501).json({ok:false,error:'GPU generation is not connected in V6 trial. Configure the server-side worker adapter before spending credits.'})});const port=process.env.PORT||3000;app.listen(port,()=>console.log(`AHM Studio V6 running on ${port}`));
