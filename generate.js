export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});return res.status(501).json({ok:false,mode:'development',message:'GPU generation is not connected yet. AHM Studio is ready for the RunPod/ComfyUI worker.'})}